/**
 * ProtectedRoute — currently bypassed (no auth required).
 * Will be reconnected when Supabase auth is wired up.
 */
export default function ProtectedRoute({ children }) {
  return children;
}
